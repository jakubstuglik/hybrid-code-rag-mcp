# hybrid-code-rag-mcp dependencies

# Core RAG framework
llama-index==0.14.15
llama-index-core==0.14.15
llama-index-cli==0.5.3

# Vector store
qdrant-client==1.17.0
llama-index-vector-stores-qdrant==0.9.1

# Embeddings
llama-index-embeddings-huggingface==0.6.1
sentence-transformers==5.2.3
fastembed==0.7.4
onnxruntime-gpu==1.24.3
einops==0.8.2  # required by nomic-ai/CodeRankEmbed custom architecture

# Jina embedding model compatibility — do NOT upgrade past 5.x.
# jinaai/jina-embeddings-v2-base-code uses a custom JinaBertModel that is
# incompatible with transformers >= 5.0 (loads as generic BertModel with
# random weights, producing garbage embeddings with no error).
# Minimum 4.48.0 required for Alibaba-NLP/gte-modernbert-base (ModernBERT
# architecture was added in transformers 4.48.0).
transformers==4.48.3
huggingface-hub==0.36.2
tokenizers==0.21.4

# Parsers and code analysis
tree-sitter==0.25.2
tree-sitter-language-pack==0.13.0

# File handling
pymupdf==1.27.1
pypdf==6.7.4
watchdog==6.0.0
beautifulsoup4==4.14.3

# System monitoring
psutil==7.2.2

# Validation test suite (YAML test definitions)
PyYAML==6.0.3

# MCP (Model Context Protocol)
mcp==1.26.0
