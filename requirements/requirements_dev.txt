# Development dependencies (testing, linting, formatting)
# Install with: uv pip install -r requirements/requirements_dev.txt

# Testing
pytest==9.0.2
pytest-cov==7.0.0

# Linting
ruff==0.15.5

# Formatting
black==26.3.0

# Type checking (optional — not all code is fully typed yet)
mypy

# Pin chardet to a version compatible with requests==2.32.5
# (requests checks chardet >= 3.0.2, < 6.0.0 at import time).
# chardet 7.x is pulled by diff-cover/sqlfluff and triggers
# RequestsDependencyWarning on every import.
chardet>=5.0,<6
